# ud-networking-for-web-developers
Some utilities I built while learning Networking for Web Developers at Udacity

    docker build -t udnet .
    docker run --rm -it udnet bash
    docker ps
    docker exec -it <container-id> bash
